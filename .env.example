# Copy to .env and fill in. Never commit .env.

# Anthropic API key for extraction and (optionally) judge stages
ANTHROPIC_API_KEY=

# Ollama host for local LLM provider (used for judge stage where latency/cost matters)
OLLAMA_HOST=http://localhost:11434
